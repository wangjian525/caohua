version: 1
formatters:
  simple:
    format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
    formatter: simple
    stream: ext://sys.stdout
  file:
    class: logging.handlers.TimedRotatingFileHandler
    level: DEBUG
    formatter: simple
    filename: ./config/modelservice.log
    when: "D"
    interval: 1
loggers:
  main:
    level: DEBUG
    handlers: [console, file]
root:
  level: DEBUG
  handlers: [console, file]